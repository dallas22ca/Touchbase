<h1>Emails</h1>

<table id="contacts">
  <thead>
    <tr>
      <th>Subject</th>
      <th style="width: 15%; " class="center"># Rcps</th>
    </tr>
  </thead>
  
  <tbody>

    <% @emails.each do |email| %>
      <tr>
        <td class="name"><%= link_to email.subject, email %></td>
        <td class="center"><%= link_to email.tasks.count, email %></td>
      </tr>
    <% end %>
    
  </tbody>
</table>
