<%= form_for(@followup) do |f| %>
  <% if @followup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@followup.errors.count, "error") %> prohibited this followup from being saved:</h2>

      <ul>
      <% @followup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :criteria %><br>
    <%= f.text_area :criteria %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :date_offset %><br>
    <%= f.number_field :date_offset %>
  </div>
  <div class="field">
    <%= f.label :recurring %><br>
    <%= f.check_box :recurring %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
